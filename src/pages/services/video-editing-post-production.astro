---
import ServiceLayout from '../../layouts/ServiceLayout.astro';
import { services } from '../../data/services';
const service = services.find((s) => s.slug === 'video-editing-post-production')!;
---
<ServiceLayout {...service} />
